server:
  port: "3000"
  env: "development"
  read_timeout: 30s
  write_timeout: 30s
  domain: "localhost"

database:
  driver: "mysql"  # 或 "postgres"
  host: "localhost"
  port: "3306"     # MySQL 默认端口 3306，PostgreSQL 默认 5432
  user: "root"
  password: "123456"
  name: "magnet_webdav"

torrent:
  download_dir: "./data/torrents"
  cache_size: 1073741824
  max_connections: 100
  user_agent: "Magnet-WebDAV/1.0"
  listen_port: 0

auth:
  enabled: true   # 设置为true启用认证
  username: "admin"
  password: "password123"
